import HeroSection from '@/components/Home/HeroSection';
import React from 'react';

const page = () => {
    return (
        <div>
            <HeroSection />
        </div>
    );
};

export default page;